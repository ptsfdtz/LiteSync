# LiteSync

LiteSync 是一个跨平台桌面备份工具，目标是在最小打扰下将用户选定的源目录持续同步到目标目录。

> 当前仓库为文档先行阶段，核心功能均为**规划中**。

## 项目简介

- 一句话价值主张：在 Windows/macOS/Linux 上，为个人与小团队提供“低打扰、可恢复、可观测”的目录自动备份体验。

## 技术选型（为何 Go 而非 Rust）

- 结论：当前阶段主技术栈确定为 `Go + Fyne`。
- 选型理由：
  - 开发效率：Go 对并发任务（监听、队列、同步执行）表达直接，MVP 落地速度更快。
  - 跨平台桌面：Fyne 与 Go 的组合可覆盖 Windows/macOS/Linux，工程链路清晰。
  - 分发与维护：Go 单二进制发布成本低，便于快速验证和迭代。
  - 团队协作：对于“先可用后优化”的备份产品，Go 的学习与维护曲线更平衡。
- Rust 保留策略（规划中）：
  - 若未来出现明确性能瓶颈，可将同步核心局部模块化为 Rust 实现，通过进程/接口集成。

## 核心特性

- 可视化选择备份源目录与目标目录（规划中）
- 首次全量同步 + 后续增量同步（规划中）
- 文件变化监听自动触发同步（规划中）
- 开机自启动、后台运行、最小化打扰（规划中）
- 异常可恢复：失败重试、任务状态可追踪（规划中）
- 日志分级与问题排查支持（规划中）

## 适用平台与技术栈

- 平台：
  - Windows 10/11（规划中）
  - macOS 12+（规划中）
  - Linux（主流桌面发行版，规划中）
- 技术栈：
  - `Go`：业务逻辑、并发调度、文件同步引擎
  - `Fyne`：跨平台桌面 UI、托盘交互（规划中）
  - `YAML`：本地配置文件格式

## 计划中的关键能力

- 自动备份：
  - 新建任务后执行首次全量同步
  - 监听到变更后执行增量同步
- 文件变更监听：
  - 事件驱动（Watcher）+ 周期校验（Reconcile）双机制
- 静默启动：
  - 支持开机自启动与后台托盘运行
- 可恢复：
  - 中断后重启恢复任务状态（规划中）
  - 冲突文件保底策略（默认建议：先备份后覆盖）

## 快速开始

> 当前仓库已完成项目骨架初始化，核心同步能力仍为规划中。

### 1) 环境准备

- Go `1.22+`（建议）
- 支持平台：Windows/macOS/Linux

### 2) 初始化默认配置

```bash
go run ./cmd/litesync -init-config
```

### 3) 构建

```bash
go build -o bin/litesync ./cmd/litesync
```

### 4) 运行

```bash
go run ./cmd/litesync
```

### 5) 目录说明（当前）

```text
LiteSync/
  AGENTS.md
  README.md
  configs/
    config.example.yaml
  docs/
    ARCHITECTURE.md
    API.md
    CONFIG.md
    FAQ.md
    ROADMAP.md
    TODO.md
  cmd/
    litesync/         # 应用入口（规划中）
  internal/
    backup/           # 同步与冲突处理（规划中）
    watcher/          # 文件监听（规划中）
    scheduler/        # 事件/定时调度（规划中）
    config/           # 配置管理（规划中）
    startup/          # 开机自启（规划中）
    logx/             # 日志模块（规划中）
```

## 文档导航

- [Agent 启动文档（AI 专用）](AGENTS.md)
- [架构设计](docs/ARCHITECTURE.md)
- [内部接口约定](docs/API.md)
- [配置规范](docs/CONFIG.md)
- [开发路线图](docs/ROADMAP.md)
- [任务清单](docs/TODO.md)
- [常见问题](docs/FAQ.md)

### AI 开发入口

- 若在任意设备上使用 Agent 工具开始开发，请先阅读 `AGENTS.md`，该文件定义了统一术语、启动顺序、实现优先级与文档同步规则。

## 开发里程碑概览

| 阶段 | 目标 | 核心交付 |
| --- | --- | --- |
| MVP | 可用版本 | 单任务目录同步、监听触发、基础日志、自启动开关 |
| Beta | 稳定性版本 | 多任务、冲突策略、恢复能力、跨平台验证 |
| v1.0 | 发布版本 | 安装与升级体验、性能优化、完善文档与发布流程 |

详细计划见 [docs/ROADMAP.md](docs/ROADMAP.md)。

## 风险与边界说明

- 跨平台权限差异：
  - 系统目录、外接盘、网络挂载目录可能存在权限或事件丢失问题。
- 文件监听限制：
  - 文件系统事件可能丢失、合并或延迟，必须配合周期校验机制。
- 占用文件与原子写入：
  - 正在被其他进程写入的文件可能无法立即同步，需要重试与退避。
- 非目标场景边界（当前）：
  - 不提供云端同步
  - 不做双向同步（以源目录为准的一方向备份）
  - 不保证跨机器的实时一致性

## 状态声明

- 当前状态：骨架已初始化，核心业务能力开发中
- 代码实现状态：
  - 已完成：项目结构、配置加载与校验骨架、模块接口占位、基础启动流程
  - 未完成：文件监听、同步引擎、自启动落地、托盘交互等核心功能
- 约定有效性：本文档与 `docs/` 下设计文档保持一致，后续变更需同步更新。
